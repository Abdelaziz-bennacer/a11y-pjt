import 'package:flutter/material.dart';

class AdminCompaniesView extends StatelessWidget {
  const AdminCompaniesView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('OK!'),);
  }
}
